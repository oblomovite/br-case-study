# Instructions to setup and run this project

## Install the dependencies

```shell
cd backend & pip install -r requirements.txt
cd frontend & npm install
```

## Run the app

```shell
cd frontend & npm run start:both
```
